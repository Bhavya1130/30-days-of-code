#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    int a, b;
    cin >> a >> b;
    int c;
    c = pow(2, b);
    int d = a & c;
    if(d != 0){
        cout << "Bit is 1";
    }
    else
        cout << "Bit is 0";
    return 0;
}
